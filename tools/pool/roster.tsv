# role	agent	model	effort	tier
# A PR may only be reviewed by a row whose tier is strictly greater than the
# tier that built it. That keeps a model from clearing its own work while
# still letting one provider fill both roles at different effort levels.
#
# Orca's worker-start --model/--effort covers Claude, Codex and Cursor only.
# Antigravity's model is declared here but configured on its own side; Orca
# cannot pin it per launch.
#
# Every `agent` below must be one `orca agent hooks status` reports as
# `installed`, not merely a binary on PATH. `worker-start --agent <id>`
# rejects anything else with `agent_unconfigured`, which costs a claim every
# scheduled run. That command also lists ids Orca merely KNOWS -- cursor,
# gemini, amp and others are `not_installed` -- so "Orca has heard of it" is
# not the bar. tests/pool/test_roster.sh enforces this for every row.
#
# Tier 1 takes the mechanical, well-specified issues. Tier 2 takes the ones
# whose difficulty is algorithmic rather than large: AI steering, simulation
# budgets, physics integration, device profiling. Routing is by `agent:<id>`
# label on the issue; unlabelled issues fall to the lowest tier.
implement	antigravity	3.7-flash	-	1
implement	claude	opus	high	2
implement	codex	gpt-5.5	high	2
review	claude	opus	max	3
