# Schemas

This directory will hold validation schemas for AI-assisted asset source files.

The first schema is expected to describe vector source specifications for
ships, asteroids, and bullets. Until that schema exists, contributors should use
the required provenance fields in [the asset pipeline contract](../../docs/asset-pipeline.md).
